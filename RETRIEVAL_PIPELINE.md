# 检索流程详解（Retrieval Pipeline）

> 本文件完整说明设备维修知识系统中"故障描述 → 知识检索 → AI 回答"的检索链路：
> 每一步的**计算方法、设计原因、带来的好处**，附真实数据演练与已知问题记录。
>
> 涉及代码：
> - 入口逻辑：[guided_repair_agent.py](../backend/app/agents/guided_repair_agent.py)
> - 检索工具集：[tools.py](../backend/app/agents/tools.py)
> - 关键词提取：[query_extractor.py](../backend/app/agents/query_extractor.py)
> - 向量库封装：[vector_store.py](../backend/app/core/vector_store.py)
> - 向量编码：[embeddings.py](../backend/app/core/embeddings.py)

---

## 0. 流程总览

```
用户消息 "注塑机温度过高"
        │
        ▼
[1] 查询关键词提取 QueryExtractor        —— 白名单优先 + LLM 兜底
        │  "注塑机 温度 过高"
        ▼
[2] 双路并行检索 ────────────────┬────────────────────┐
        ▼                       ▼                    │
[2.1] 向量检索                 [2.2] BM25 检索        │
  Milvus 余弦相似度         PostgreSQL ILIKE 加权    │
  top5（语义）              top5（关键词）            │
        │                       │                    │
        └───────────┬───────────┘                    │
                    ▼                                 │
[3] RRF 融合  Σ 1/(60+rank)      —— 排名倒数求和       │
                    │                                 │
                    ▼                                 │
[4] 过滤  rrf_only 且 score<0.15 剔除                  │
                    │                                 │
                    ▼                                 │
[5] 加权重排  设备×0.2 / 命中率×0.15 / ×(1+0.4×ratio)  │
                    │                                 │
                    ▼                                 │
[6] Top3 注入 Prompt → LLM 生成【分析】+【操作】────────┘
```

**整体设计哲学**：三阶段分工——

| 阶段 | 负责 | 解决什么问题 |
|---|---|---|
| 召回（2） | 把可能相关的都捞进来 | 漏检 |
| 融合（3-4） | 统一多路分数、清掉噪音 | 分数不可比、假命中 |
| 精排（5-6） | 按真实相关度定胜负 | 排名太粗糙 |

---

## 1. 查询关键词提取（QueryExtractor）

**怎么计算**

1. 构建白名单（缓存 5 分钟）：内置设备词 + 故障信号词 + 从 DB 加载所有 `device_type` 和 `fault_tags`，实测共 **151 词**
2. 按词长降序在用户消息中匹配（长词优先，避免"PLC"吃掉"PLC系统"）
3. 补提英文/数字 token（`PLC`、`RAM`、故障码 `6101`）
4. 命中 ≥1 个 → 空格拼接返回；**0 命中** → 调 DeepSeek 提取关键词（结果缓存 10 分钟）

**为什么这样计算**

- 白名单来自知识库本身的真实标签（device_type / fault_tags），词表与业务数据同源，命中率天然高
- 中文没有空格，关键词提取是后续一切匹配的基础
- 0 命中才动用 LLM，控制成本与延迟

**好处**

- 先过滤掉口语干扰词（"请帮我看看…"），让 embedding 和 ILIKE 都针对纯技术词，避免语义向量被噪声带偏
- 实测：`注塑机温度过高 → 注塑机 温度 过高`

---

## 2. 双路并行检索

### 2.1 向量检索（语义路）

**怎么计算**

1. 查询文本编码为 **1024 维单位向量**（[embeddings.py](../backend/app/core/embeddings.py)）：
   - Qwen3-Embedding-0.6B 本地加载（CUDA）
   - tokenize 后取**最后一层 EOS token 的 hidden state** 作为整句向量
   - **L2 归一化** → 向量长度压成 1
2. Milvus 按 `COSINE` 度量搜索（索引 `IVF_FLAT`）：
   - 因两边都已归一化，**余弦相似度 = 点积**

     ```
     cos(θ) = (query · 知识) / (|query| × |知识|) = query · 知识
     ```

   - 范围 [-1, 1]：1 语义完全一致，0 无关，负数相反
   - 取 top5

**为什么这样计算**

- 余弦相似度衡量"语义方向一致性"，对同义词/近义表达鲁棒（"温度过高"和"温度偏高"向量接近）
- 归一化是为了让余弦度量直接用点积实现，Milvus 上计算快、索引友好

**好处**

- 召回"意思对但字不对"的案例，是纯关键词匹配做不到的
- 实测：`注塑机温度过高` 与 `【注塑机】模具温度失控的处理方法` 余弦 = **0.7288**，与射胶量案例 = 0.6095，语义区分明显

### 2.2 BM25 检索（关键词路）

**怎么计算**

1. 提取后的关键词做 **2-gram / 3-gram 重叠切分**（近似中文分词，无 jieba）：

   ```
   "注塑机温度过高" → {注塑机, 注塑, 塑机, 温度, 过高}
   ```

2. 分类：命中设备词表 → 设备词；其余 → 故障词
3. 生成 SQL：每个 token 一个 `ILIKE` 条件，**OR 连接**（任一命中即召回）

   ```sql
   WHERE (title ILIKE '%温度%' OR content ILIKE '%温度%')
      OR (title ILIKE '%过高%' OR content ILIKE '%过高%') ...
   ```

4. **加权计分**（CASE WHEN 累加，按 token 类别）：

   | 类别 | 命中标题 | 命中内容 |
   |---|---|---|
   | 故障词 | +5 | +2 |
   | 设备词 | +1 | +1 |

5. 按总分降序取 top5；SQL 异常时降级为纯 `LIKE` 匹配

**为什么这样计算**

- **ILIKE** = PostgreSQL 大小写不敏感模糊匹配，`%` 通配任意长度，即"包含即命中"；对英文 token（`PLC`）也不挑大小写
- **OR 扩大召回**：只要沾边就捞进来，宁多勿漏——召回阶段允许噪音，由后段过滤负责清理
- **2-gram 切分**：中文无空格，切词能拆出"注塑/塑机"等片段，覆盖复合词变体
- **故障词权重大于设备词**（5/2 vs 1/1）：故障描述是相关性最核心的信号，设备只是定位上下文——这是项目既定策略

**好处**

- 精确词命中、零延迟、不吃语义模型资源
- 与向量路互补：**BM25 管"词对上"，向量管"义对上"**

### 2.3 BM25 性能优化：pg_trgm 索引（已实施）

> 目标：让 `ILIKE '%关键词%'` 模糊匹配在上千条知识后依然毫秒级，避免退化为全表扫描。

**为什么做（问题背景）**

- BM25 的 SQL 是 `title ILIKE '%词%' OR content ILIKE '%词%'`——`%词%` 意味着"任意位置包含"，位置不确定
- 没有索引时，PostgreSQL 只能**逐行全文扫描**（复杂度 O(n)）：几百条时毫秒级无感，到**上万条会明显变慢**
- 向量路靠 Milvus ANN 索引天然可扩展，**BM25 是全链路唯一随数据量恶化的环节**，必须提前铺路

**为什么普通索引不行**

- 数据库默认的 B-tree 索引只擅长"精确值 / 前缀"匹配（`title = 'xx'` 或 `LIKE 'xx%'`），对中间的 `%xx%` 无能为力
- 需要专门为"子串包含匹配"设计的工具：**pg_trgm**

**怎么实现（原理）**

1. **建索引时**（一次性）：把每段文本切成**连续的 3 字片段（trigram）**

   ```
   "注塑机温度过高" → 注塑机, 塑机温, 机温度, 温度过, 度过高
   ```

2. 用 **GIN 倒排索引**登记：每个 trigram → 包含它的所有行号

   ```
   "注塑机" → {第5行, 第89行, ...}
   ```

3. **查询时**：查询词切 trigram → 查倒排索引直接拿到候选行号（哈希定位，O(1)）→ 只对候选行做精确 ILIKE 验证（防假命中）

   ```
   10000 行全扫  →  索引锁定几十行候选  →  只验证这几行
   ```

**益处**

- 查询复杂度从 **O(n)（全表扫描）降到 O(候选集)**，数据越多差距越大
- 写入代价极小（每次插入只登记几个 trigram，毫秒级）；知识库"读多写少"，非常划算
- 与向量路的 Milvus ANN 索引形成对称：**两条检索路都有索引支撑，数据增长不拖累任何一路**

**实现细节（已落地）**

- Alembic migration `7a8b9c0d1e2f`（可回滚）：
  ```sql
  CREATE EXTENSION IF NOT EXISTS pg_trgm;
  CREATE INDEX idx_knowledge_title_trgm ON knowledge_items USING gin (title gin_trgm_ops);
  CREATE INDEX idx_knowledge_content_trgm ON knowledge_items USING gin (content gin_trgm_ops);
  ```
- 已验证：强制走索引时执行计划显示 `Bitmap Index Scan on idx_knowledge_title_trgm / idx_knowledge_content_trgm`

**边界与注意**

- **查询词 ≥3 字提速最明显**（有 trigram 可拆）；2 字词（如"温度"）没有 trigram，会部分退化——BM25 的 2-gram/3-gram 混合切词恰好长短互补
- **数据量小（当前 25 条）时优化器自动选全表扫描**——全扫比索引便宜，这是正常的；数据量上千后优化器会**自动**改用 Bitmap Index Scan，无需改动代码

---

## 3. RRF 融合（Reciprocal Rank Fusion）

**怎么计算**

```
某条知识最终分 = Σ 1 / (k + rank_i)        k = 60
                  i=所有检索路
```

- `rank_i`：该知识在第 i 路的排名（第 1 名 rank=1）
- 每路**只按排名**贡献分数，与原始分值无关

**为什么这样计算**

两路检索的**分数量纲完全不同，不能直接相加**：

| 路 | 分数含义 | 范围 |
|---|---|---|
| 向量 | 余弦相似度 | 0 ~ 1 |
| BM25 | 加权计数累加 | 0 ~ 几十 |

直接相加向量路会被淹没。**排名是相对序**（第 1 名就是比第 2 名好），两路天然可比。

**参数 k=60 的作用**：平滑/稀释系数。k 越大，排名差距影响越小（第 1 名 1/61≈0.0164，第 2 名 1/62≈0.0161，几乎拉不开）——**刻意削弱单路"第一名光环"，让双路命中的知识有机会胜出**。

**为什么是 60（有论文依据）**：
- 来源：RRF 原论文 *"Reciprocal Rank Fusion Outperforms Condorcet and Individual Rank Learning Methods"*（Cormack, Clarke & Büttcher, ACM SIGIR 2009）——RRF 的出处就是这篇，作者在 pilot 实验阶段固定 k=60 并在后续验证中不再调整，原文称其为 **near-optimal，但 choice was not critical（选值不敏感）**
- 论文原话：k 的作用是 *"mitigates the impact of high rankings by outlier systems"*（抑制离群系统把某条顶到第 1 名带来的过重影响）——与本项目"降低单路独占性"的意图完全一致
- 论文表 1（TREC pilot，MAP）：k=10→0.2123，k=30→0.2139，k=60→0.2145，k=80→0.2147，k=100→0.2142，k=500→0.2098——**60~100 区间 MAP 几乎无差**，k 太大（500）才会明显变差
- 事实标准：该论文后 RRF 成为混合检索事实标准，Elasticsearch（8.9 起内置）、Qdrant、Weaviate 等默认 k=60

**k 的直觉**：k 越小，排名靠前的独占权重越高（k=1 时第 1/2 名之比 0.5 vs 0.33）；k 越大，各名次贡献趋同，**"多条检索路都认可"比"单一路排第一"更重要**——这正是本项目想要的共识效应。

**好处**

1. 公平融合异构检索器
2. **双路命中天然加分**：`1/61 + 1/62` 必大于任何单路分数，形成"词也对、义也对 → 最可信"
3. 容错：一路检索失败另一路仍能工作

**代价**：原始相似度信息被压缩成排名序，所以必须由第 5 步重排拿回分数维度。

---

## 4. 候选过滤

**怎么计算**

```python
kept = [m for m in merged
        if not m.get("rrf_only", False) and m.get("score", 0) >= 0.15]
```

- `rrf_only=True`：该知识**只有 BM25 命中、向量路没有召回** → 显示分=0
- 剔除 `rrf_only` 且向量分 < 0.15 的条目

**为什么这样计算**

- BM25 独中的条目 = "关键词碰巧撞上，但语义上向量路不认可"，是 OR 召回捞进来的典型噪音（实测：`【空压机】排气温度过高报警` 同时含"温度+过高"俩故障词，BM25 排第一，但和"注塑机"无关）
- 向量分 < 0.15 = 语义几乎不相关

**好处**

- 用"向量语义认可"作为硬门槛，把关键词撞车的假命中挡在门外
- 全链路四道防线之一（RRF 清零 → 此处过滤 → 重排压分 → Top3 截断）

---

## 5. 加权重排（Weighted Rerank）

**怎么计算**

1. 从提取词中分离两类（词表来源：`_DEVICE_TYPE_TERMS` 硬编码 18 个设备词 + `cleaned_query` 拆分）：

   ```
   device_kw = {注塑机}          ← 命中硬编码设备词表 _DEVICE_TYPE_TERMS（tools.py:29）
   fault_kw  = {温度, 过高}      ← 剩余技术词（剔除通用词"故障"及 device_kw），兜底用 _FAULT_CAUSE_SIGNALS
   ```

2. 对每条知识算两个指标：

   ```
   fault_hit_count = fault_kw 中出现在 标题+正文 的词个数
   fault_ratio     = fault_hit_count / len(fault_kw)      ← 覆盖率
   device_match    = 该条 device_type 是否包含 device_kw 的词
   ```

   **ratio 取值详解**（[tools.py:393](file:///d:/WeiZhi%20Works/backend/app/agents/tools.py#L393)）：
   - `fault_hit_count` 用**子串包含匹配**（`kw in title+content`），每个故障词最多计 1 次（重复出现不累计），避免长文本靠重复词刷分
   - **分母是用户查询的故障词数**（不是知识条目的），所以同一知识在不同查询下 ratio 会变化
   - 例（fault_kw = {温度, 过高}，len=2）：
     - 模具温度失控：正文含"温度"→ hit=1 → **ratio = 1/2 = 0.5**
     - 射胶量不稳定：不含任何故障词 → hit=0 → **ratio = 0**
   - 取值区间与分数处理：
     - `ratio = 0` → ×0.15（压地板）
     - `0 < ratio < 0.3` → ×0.15（仍按不相关处理）
     - `0.3 ≤ ratio ≤ 1` → ×(1 + 0.4×ratio) 加分，最多 +40%
     - `ratio = 1`（全部命中）→ 加分封顶

3. 三分支修正（基数为 RRF 后的向量余弦分）：

   | 条件 | 公式 | 含义 |
   |---|---|---|
   | 设备不匹配 | `×0.2` | 设备都对不上，语义再近也不可信 |
   | 故障命中 0 或 ratio < 0.3 | `×0.15` | 只沾边、实质无关 → 压到地板 |
   | ratio ≥ 0.3 | `×(1 + 0.4×ratio)`，上限 1.0 | 故障概念覆盖越多，加越多，最多 +40% |

**为什么这样计算**

- **RRF 只认排名丢了分数**，这一步拿回真实相关度做精细校正
- **设备是门槛、故障是增益**：设备不匹配直接 ×0.2，故障命中只在上限内加分
- **用比率不用绝对数**（标准做法）：查询故障词数量不等（2 个 vs 4 个），绝对命中数不可比，归一化后才跨查询可比
- **0.3 / 0.4 / 0.15 三个系数**是项目结合业务调出的启发式经验值，无权威标准，属于"有依据的经验设计"
- **上限 1.0**：防止加分超过语义余弦理论上界

**好处**

- 同一设备下，真正描述当前故障的案例被抬升，沾边案例被压死
- 实测："模具温度失控"命中故障词 1/2 → `0.7288 × 1.2 = 0.8746`；射胶量/电机异响命中 0 个 → `0.6095×0.15=0.0914`、`0.5677×0.15=0.0852`，胜负立判

**数据来源（对比方向）**

- **基准**：用户描述里提取的 设备词(device_kw) + 故障词(fault_kw)
- **被检对象**：知识库候选条目（title / content / device_type）
- 对比方向是"用户的词当基准、知识条目当被检对象"，两条线机制不同：
  - **故障词线**：用户故障词 → 在知识的 `标题+正文` 里找 → 命中率决定加分幅度（油门）
  - **设备线**：用户设备词 → 与知识的 `device_type` 字段比对 → 决定放行或 ×0.2 惩罚（闸门）
- 重排**不发起新检索**，纯在过滤后的候选集上算分排序；Top3 结果存进追踪维修的 Redis 会话（`session["cases"]`）

**效率与架构（为什么这么快）**

- 重排只对比**过滤后的 3~5 条候选**，不是全库——前面的双路召回 + RRF + 过滤已把范围收窄
- 每条成本 = Python 子串匹配（微秒级）；实测 RRF 融合 + 重排合计 **2.6ms**
- 这是标准的**两阶段检索架构**：粗排（召回：广撒网、快）→ 精排（重排：精细、贵计算给最少候选）

**涉及的知识点**

1. **两阶段检索架构**（Cascade / Two-stage Retrieval）：召回广撒网、精排精打分
2. **查询覆盖率**（Query Coverage）：`fault_ratio = 命中词数 / 查询词数`，归一化后跨查询可比（IR 标准思想）
3. **领域词表设计**：设备词 vs 故障词的语义分离——设备是定位上下文（权重 1），故障是核心信号（权重 5）
4. **启发式系数**（0.3 / 0.4 / 0.15）：无权威标准，业务经验调参，属"有依据的经验设计"
5. **复杂度分析**：O(候选条数 × 故障词数 × 文本长度)，与全库规模无关
6. **工程权衡**：宁可给 LLM 一条弱相关案例（prompt 强制"只能引用案例明确内容"），也不漏掉潜在相关案例

---

## 6. Top3 输出与 LLM 生成

> 注：本节以追踪维修为例取 Top3；**其他入口数量不同**——钉钉检索取 Top5（mcp/tools.py:105），问答/搜索页取前端传入的 top_k（默认 8）。规律：注入 LLM 的少而精，展示给用户浏览的多。

**各入口重排后的实际取值**：

| 入口 | 重排后取几条 | 代码位置 | 用途 | 为什么这个数 |
|---|---|---|---|---|
| 追踪维修 `_search_knowledge` | **3 条** | `merged[:3]`（guided_repair_agent.py:201） | Top3 注入 LLM 生成下一步排查 | 对话式引导只需最相关的几条，多了干扰 LLM 判断 |
| 钉钉知识检索 `search_knowledge` | **5 条** | `[:5]`（mcp/tools.py:105） | Top5 交给 AnswerAgent 生成回答 | 回答要综合参考，稍多几条 |
| 问答/搜索端点（/answer、/search、/agent） | **top_k 条**（前端默认 8） | `rrf_merge(top_n=request.top_k)`，不额外截断 | 展示给用户浏览的案例列表 | 用户要浏览多条 |

- 重排后取前 3，content 截断 600 字
- 作为"知识库参考（唯一知识来源）"注入 Prompt
- 强制 LLM：**只能引用案例明确写到的内容**（灯名/部件名/数值不得补充），未检索到则如实回"知识库暂无相关案例"
- 实测：AI 回复中"模温机显示温度与实际偏差>10℃"与该案例内容逐字一致，未自由发挥

---

## 7. 完整实例演练（真实数据）

查询：**注塑机温度过高**

| 阶段 | 输出 |
|---|---|
| [A] 关键词提取 | `注塑机 温度 过高`（白名单命中） |
| [B] 向量路 top5 | 模具温度失控 0.7288 / 射胶量 0.6095 / 电机异响 0.5677 / 开合模 0.5512 / 液压压力 0.5325 |
| [C] BM25 路 top5 | 空压机排气温度过高 12.0（**噪音**）/ 模具温度失控 10.0 / 其余 3.0 |
| [D] RRF 融合 | 模具温度失控 0.0325（双路合并）/ 空压机 0.0164（rrf_only） |
| [E] 过滤 | 剔除空压机噪音 + 重复副本，剩 3 条 |
| [F] 重排 | 模具温度失控 0.8746 / 射胶量 0.0914 / 电机异响 0.0852 |
| [G] Top3 → LLM | 仅第 1 条被真正引用 |

---

## 8. 已知问题与修复记录

### 8.1 RRF 合并键不一致 Bug（已修复）

**问题**：[tools.py](../backend/app/agents/tools.py) 的 RRF 原用 `id` 做合并键，但：
- 向量路的 `id` = Milvus point_id（uuid 字符串）
- BM25 路的 `id` = 数据库知识 id（整数）

同一知识两路 `id` 不同 → 被当成两条，**双路命中分不合并**（实测模具温度失控拆成 0.0164+0.0161，而非合并的 0.0325），排序精度受损。

**修复**：合并键改为优先 `knowledge_id`（知识库主键，跨路稳定）。

**防范措施**：
1. **一致性告警**：某路返回的 `id` 与 `knowledge_id` 不一致时打 WARNING（每路仅一次），提示新增检索路必须带 `knowledge_id`
2. **防御性去重**：返回前兜底按 `knowledge_id` 去重，防止未来回归
3. **docstring 写明去重键约定**，维护者一看便知

---

## 9. 关键参数速查

> 取值依据按来源分为四类：**① 论文/标准依据** ② **行业惯例/事实标准** ③ **项目业务策略**（记忆库中既定规则）④ **工程取舍/经验值**

| 参数 | 值 | 位置 | 含义 | 取值依据 |
|---|---|---|---|---|
| RRF 平滑系数 k | 60 | `rrf_merge` | 排名→分数映射斜率 | ① Cormack 等 SIGIR 2009 论文 pilot 固定值，60~100 近最优；② Elasticsearch/Qdrant 等默认值 |
| 向量路 top_k | 5 | 各入口 | 向量召回数 | ④ 工程取舍：语义近邻取前 5 已覆盖，过大增加编码+比对延迟 |
| 向量召回阈值 | 0.0 | `vector_search` | 语义最低线 | ④ 召回阶段宁多勿漏，交给下游过滤 |
| 过滤阈值 | 0.15 | 各入口过滤 | 语义认可门槛 | ④ 经验值：余弦 <0.15 视为基本无关（低于此向量路也排不进 top5） |
| BM25 top_k | 5（追踪维修）/ 10（默认及其他入口） | `bm25_search` | 关键词召回数 | ④ 追踪维修取 5 够用（最终截到 top3）；问答/搜索页要展示案例列表取 10 |
| pg_trgm 索引 | GIN(title/content) | 迁移 `7a8b9c0d1e2f` | 加速 ILIKE 模糊匹配 | ③ 数据上千条后避免全表扫描（详见 2.3） |
| 故障词权重（标题/内容） | 5 / 2 | tools.py | 故障命中加分 | ③ 项目既定策略：故障描述是相关性核心信号；标题比正文凝练更可信 |
| 设备词权重（标题/内容） | 1 / 1 | tools.py | 设备命中加分 | ③ 设备只是定位上下文，权重刻意压低 |
| 设备不匹配惩罚 | ×0.2 | `weighted_rerank` | 硬惩罚 | ③ 业务判断：设备类型都对不上，可信度削 80% |
| 故障低命中惩罚 | ×0.15 | `weighted_rerank` | 压到地板 | ③ 接近归零（保留极小分避免系统性误杀） |
| 故障命中增益 | ×(1+0.4×ratio) | `weighted_rerank` | 最多 +40% | ③ 项目既定加权公式 |
| 覆盖率断点 | 0.3 | `weighted_rerank` | ≥30% 才给正反馈 | ④ 经验值：覆盖不足 1/3 概念视为基本不相关 |
| 重排后 Top | 3（追踪维修）/ 5（钉钉）/ top_k（问答搜索页，默认 8） | 各入口 | 重排后取几条 | ④ 注入 LLM 的少而精（3~5 防干扰），展示给用户浏览的多（8）；详见第 6 节表格 |
